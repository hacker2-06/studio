// This layout is intentionally left empty to remove the OTT functionality.
// You can delete this file and the (ott) directory if no longer needed.
import type { ReactNode } from 'react';

export default function OttLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
